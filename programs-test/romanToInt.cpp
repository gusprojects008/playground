#include <unistd.h>
#include <unordered_map>
#include <cstring>

// using namespace std;

class Solution {
  public:
    int romanToInt(const std::string& s) { // Or "const char* s" or "const char*& s"
      std::unordered_map<char, int> roman {
        
      };
    };
};

int main() {
  Solution SolutionObj // Creates an object/instance/realization of the plan/project of an object called Solution (which is a class, but is not yet defined as an object in memory, so it is just a plan/project of an object)
  
}
